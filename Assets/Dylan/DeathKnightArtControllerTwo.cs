using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DeathKnightArtControllerTwo : MonoBehaviour
{
    // References
    public Animator anim; // AnimationController, where we set the control parameters

    public DeathKnight mainControl; 

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
